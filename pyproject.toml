[project]
name = "learn-claude-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.98.1",
    "dotenv>=0.9.9",
    "pyyaml>=6.0.3",
]
